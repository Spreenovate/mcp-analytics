<% content_for(:title, "mcp-analytics | web analytics. no dashboard. just answers.") %>
<% content_for(:body_class, "home") %>

<% content_for :head do %>
  <%= render "layouts/fonts" %>

  <style>
    /* Scoped to body.home so other pages keep using application.css. */
    body.home {
      --bg: #ffffff; --fg: #0a0a0a; --accent: #00FF88; --border: 3px;
      background: var(--bg); color: var(--fg);
      font-family: "JetBrains Mono", ui-monospace, Menlo, Consolas, monospace;
      font-size: 16px; line-height: 1.55; -webkit-font-smoothing: antialiased;
    }
    body.home main { max-width: none; padding: 0; margin: 0; }

    body.home .wrap { max-width: 1080px; margin: 0 auto; padding: 32px 28px 120px; }

    body.home header.topnav {
      display: flex; justify-content: space-between; align-items: baseline;
      border-bottom: var(--border) solid var(--fg);
      padding-bottom: 20px; margin-bottom: 72px;
    }
    body.home header.topnav .brand {
      font-family: "JetBrains Mono", monospace; font-weight: 700;
      font-size: 20px; letter-spacing: -0.01em;
    }
    body.home header.topnav .brand::before { content: "> "; color: var(--accent); }
    body.home header.topnav .brand .tag {
      font-size: 12px; font-weight: 400; text-transform: uppercase;
      letter-spacing: 0.12em; color: #555; margin-left: 10px;
      padding-left: 10px; border-left: 1.5px solid #ccc;
    }
    body.home header.topnav nav a {
      font-family: "JetBrains Mono", monospace; font-size: 13px;
      color: var(--fg); text-decoration: none; text-transform: uppercase;
      letter-spacing: 0.08em; margin-left: 24px;
    }
    body.home header.topnav nav a:hover { background: var(--accent); }

    body.home .hero { isolation: isolate; }
    body.home .hero h1 {
      font-family: "Space Grotesk", ui-sans-serif, system-ui, sans-serif;
      font-weight: 900; font-size: clamp(52px, 9vw, 120px);
      line-height: 0.92; letter-spacing: -0.03em; margin: 0 0 40px;
      text-transform: uppercase; position: relative;
    }
    body.home .hero h1 .accent { position: relative; color: var(--fg); }
    body.home .hero h1 .accent::before {
      content: ""; position: absolute; z-index: -1;
      top: -0.05em; bottom: -0.05em; left: -0.15em; right: -0.05em;
      background: var(--accent);
    }
    body.home .hero .sub {
      max-width: 640px; font-size: 18px; line-height: 1.55; margin: 0 0 56px;
    }

    body.home .signup {
      border: var(--border) solid var(--fg); padding: 32px;
      margin: 0 0 96px; max-width: 640px;
    }
    body.home .signup .label {
      font-size: 12px; font-weight: 700; text-transform: uppercase;
      letter-spacing: 0.12em; margin-bottom: 12px; display: inline-block;
      background: var(--fg); color: var(--bg); padding: 4px 8px;
    }
    body.home .signup h2 {
      font-family: "Space Grotesk", sans-serif; font-weight: 900;
      font-size: 28px; margin: 8px 0 20px;
      text-transform: uppercase; letter-spacing: -0.01em;
    }
    body.home .signup p.help { margin: 0 0 20px; font-size: 14px; color: var(--fg); }
    body.home .signup form {
      display: flex; gap: 0; border: var(--border) solid var(--fg);
    }
    body.home .signup input[type=email] {
      flex: 1; border: 0; outline: 0; background: var(--bg); color: var(--fg);
      font-family: "JetBrains Mono", monospace; font-size: 16px;
      padding: 16px 18px; border-right: var(--border) solid var(--fg);
      -webkit-appearance: none; appearance: none;
      box-shadow: none; border-radius: 0;
    }
    body.home .signup input[type=email]:focus {
      background: #fffce0;  /* subtle warm focus tint, no border change */
    }
    body.home .signup input[type=email]::placeholder { color: #999; }
    body.home .signup input[type=submit] {
      border: 0; background: var(--fg); color: var(--accent);
      font-family: "JetBrains Mono", monospace; font-size: 14px;
      font-weight: 700; text-transform: uppercase; letter-spacing: 0.1em;
      padding: 16px 28px; cursor: pointer;
      -webkit-appearance: none; appearance: none;
      border-radius: 0;
    }
    body.home .signup input[type=submit]:hover { background: var(--accent); color: var(--fg); }
    body.home .signup small { display: block; margin-top: 16px; font-size: 12px; color: #666; }
    body.home .signup .alert {
      background: var(--fg); color: var(--accent);
      padding: 10px 14px; margin-bottom: 16px; font-size: 13px;
      border-left: var(--border) solid var(--accent);
    }
    body.home .signup .panel {
      border: var(--border) solid var(--fg); padding: 18px;
      margin-bottom: 16px; position: relative;
    }
    body.home .signup .panel .hint {
      float: right; font-size: 11px; color: #888;
      text-transform: uppercase; letter-spacing: 0.08em; margin-bottom: 6px;
    }
    body.home .signup .panel .copyable {
      display: block; clear: both;
      font-family: "JetBrains Mono", monospace; font-weight: 700;
      font-size: 14px; word-break: break-all;
      background: #f5f5f5; padding: 14px 16px;
      border: 1.5px solid var(--fg);
      cursor: pointer; user-select: all;
      transition: background .12s ease;
    }
    body.home .signup .panel .copyable:hover { background: var(--accent); }
    body.home .signup .panel .copyable:focus-visible {
      outline: var(--border) solid var(--accent); outline-offset: 2px;
    }
    body.home .toast {
      position: fixed; bottom: 24px; left: 50%;
      transform: translateX(-50%) translateY(8px);
      background: var(--fg); color: var(--accent);
      padding: 10px 18px;
      font-family: "JetBrains Mono", monospace; font-size: 13px;
      font-weight: 700; text-transform: uppercase; letter-spacing: 0.08em;
      opacity: 0; transition: opacity .15s ease, transform .15s ease;
      pointer-events: none; z-index: 100;
      border: 2px solid var(--accent);
    }
    body.home .toast.show { opacity: 1; transform: translateX(-50%) translateY(0); }

    body.home h2.section {
      font-family: "JetBrains Mono", monospace; font-size: 13px;
      font-weight: 700; text-transform: uppercase; letter-spacing: 0.2em;
      margin: 0 0 32px; padding-bottom: 12px;
      border-bottom: var(--border) solid var(--fg);
    }
    body.home h2.section::before {
      content: attr(data-num) " / "; color: var(--accent);
      background: var(--fg); padding: 2px 8px; margin-right: 12px;
    }

    body.home .steps {
      display: grid; grid-template-columns: repeat(3, 1fr); gap: 0;
      border: var(--border) solid var(--fg); margin-bottom: 96px;
    }
    body.home .step { padding: 28px; border-right: var(--border) solid var(--fg); }
    body.home .step:last-child { border-right: 0; }
    body.home .step .num {
      font-family: "Space Grotesk", sans-serif; font-size: 72px;
      font-weight: 900; line-height: 1; letter-spacing: -0.05em;
      margin: 0 0 16px; color: var(--fg);
    }
    body.home .step h3 {
      font-family: "JetBrains Mono", monospace; font-size: 14px;
      text-transform: uppercase; letter-spacing: 0.08em; margin: 0 0 8px;
    }
    body.home .step p { margin: 0; font-size: 14px; }

    body.home .demo {
      margin-bottom: 96px; display: grid; grid-template-columns: 1fr 1fr;
      gap: 0; border: var(--border) solid var(--fg);
    }
    body.home .demo > div { padding: 28px; min-width: 0; }  /* min-width:0 lets grid items shrink below content's natural width */
    body.home .demo > div:first-child { border-right: var(--border) solid var(--fg); }
    body.home .demo h3 {
      font-family: "JetBrains Mono", monospace; font-size: 13px;
      text-transform: uppercase; letter-spacing: 0.1em; margin: 0 0 16px;
    }
    body.home .demo pre {
      background: #f5f5f5; color: var(--fg);
      padding: 14px 16px;
      font-family: "JetBrains Mono", monospace; font-size: 12px;
      line-height: 1.5; overflow-x: auto; margin: 0;
      border: 1.5px solid var(--fg);
    }
    body.home .demo pre code { color: inherit; background: none; padding: 0; }
    body.home .chat .you, body.home .chat .claude {
      padding: 12px 14px; margin: 10px 0; font-size: 13px; line-height: 1.55;
    }
    body.home .chat .you { background: #f5f5f5; border-left: var(--border) solid var(--fg); }
    body.home .chat .claude { background: var(--accent); border-left: var(--border) solid var(--fg); }
    body.home .chat .who {
      font-weight: 700; text-transform: uppercase; letter-spacing: 0.08em;
      font-size: 11px; margin-bottom: 4px;
    }

    body.home .why {
      display: grid; grid-template-columns: 1fr 1fr; gap: 40px; margin-bottom: 96px;
    }
    body.home .why-col h3 {
      font-family: "Space Grotesk", sans-serif; font-size: 24px;
      font-weight: 900; text-transform: uppercase; letter-spacing: -0.01em;
      margin: 0 0 16px; padding: 8px 12px; display: inline-block;
    }
    body.home .why-col.lose h3 { background: var(--fg); color: var(--bg); }
    body.home .why-col.gain h3 { background: var(--accent); color: var(--fg); }
    body.home .why-col ul { list-style: none; padding: 0; margin: 0; }
    body.home .why-col li {
      font-size: 15px; padding: 10px 0;
      border-bottom: 1.5px solid var(--fg);
    }
    body.home .why-col li::before {
      content: ">"; color: var(--accent); font-weight: 700; margin-right: 10px;
    }

    body.home .pricing {
      display: grid; grid-template-columns: 1fr 1fr; gap: 0;
      border: var(--border) solid var(--fg); margin-bottom: 24px;
    }
    body.home .tier {
      min-width: 0; display: flex; flex-direction: column;
    }
    body.home .tier.free { border-right: var(--border) solid var(--fg); }
    body.home .tier.pro { background: var(--accent); }
    body.home .tier .head    { padding: 40px 40px 32px; border-bottom: 1.5px solid var(--fg); }
    body.home .tier .body    { padding: 8px 40px; flex: 1; }
    body.home .tier .foot    { padding: 32px 40px 40px; border-top: 1.5px solid var(--fg); }
    body.home .tier .name {
      font-family: "JetBrains Mono", monospace; font-size: 12px;
      font-weight: 700; text-transform: uppercase; letter-spacing: 0.14em;
      background: var(--fg); color: var(--bg); padding: 4px 8px;
      display: inline-block; margin-bottom: 20px;
    }
    body.home .tier .price {
      font-family: "Space Grotesk", sans-serif; font-weight: 900;
      font-size: 80px; line-height: 0.92; letter-spacing: -0.03em;
      margin: 0 0 10px;
    }
    body.home .tier .price .per {
      font-family: "JetBrains Mono", monospace; font-size: 14px;
      font-weight: 700; letter-spacing: 0.05em; margin-left: 6px;
      text-transform: uppercase;
    }
    body.home .tier .tagline {
      font-size: 14px; line-height: 1.5; margin: 0;
      min-height: 2.8em;  /* lock to 2 lines so Free + Pro head zones match height */
    }
    body.home .tier ul { list-style: none; padding: 0; margin: 0; }
    body.home .tier li {
      font-size: 14px; padding: 14px 0; line-height: 1.4;
      border-bottom: 1.5px solid var(--fg);
      display: flex; align-items: center;
    }
    body.home .tier li:last-child { border-bottom: 0; }
    body.home .tier li::before {
      content: ">"; color: var(--fg); font-weight: 700; margin-right: 10px;
    }
    body.home .tier li .soon {
      margin-left: auto; flex-shrink: 0;
      font-family: "JetBrains Mono", monospace; font-size: 10px;
      font-weight: 700; text-transform: uppercase; letter-spacing: 0.12em;
      background: var(--fg); color: var(--bg);
      padding: 3px 7px; white-space: nowrap;
    }
    body.home .tier li .qmark {
      display: inline-flex; align-items: center; justify-content: center;
      width: 18px; height: 18px; margin-left: 8px; flex-shrink: 0;
      background: var(--bg); color: var(--fg);
      border: 1.5px solid var(--fg);
      font-family: "JetBrains Mono", monospace; font-size: 11px;
      font-weight: 700; cursor: help; position: relative;
    }
    body.home .tier li .qmark:hover::after,
    body.home .tier li .qmark:focus::after,
    body.home .tier li .qmark:active::after {
      content: attr(data-tip);
      position: absolute; bottom: calc(100% + 10px); left: 50%;
      transform: translateX(-50%);
      background: var(--fg); color: var(--bg);
      padding: 12px 14px; font-size: 12px; line-height: 1.5;
      font-family: "JetBrains Mono", monospace; font-weight: 400;
      text-transform: none; letter-spacing: 0; text-align: left;
      width: 280px; white-space: normal;
      border: 1.5px solid var(--fg); z-index: 10; pointer-events: none;
    }
    body.home .tier .cta {
      display: block; padding: 16px 0; text-align: center;
      background: var(--fg); color: var(--accent);
      font-family: "JetBrains Mono", monospace; font-size: 13px;
      font-weight: 700; text-transform: uppercase; letter-spacing: 0.1em;
      text-decoration: none; border: var(--border) solid var(--fg);
    }
    body.home .tier .cta:hover { background: var(--bg); color: var(--fg); }
    body.home .enterprise-line {
      border: var(--border) solid var(--fg); padding: 20px 24px;
      font-size: 14px; line-height: 1.55; margin-bottom: 96px;
    }
    body.home .enterprise-line strong {
      text-transform: uppercase; letter-spacing: 0.08em; margin-right: 4px;
    }
    body.home .enterprise-line code {
      font-family: "JetBrains Mono", monospace; font-size: 13px;
      background: #f5f5f5; padding: 1px 6px; border: 1.5px solid var(--fg);
    }
    body.home .enterprise-line a { color: var(--fg); }
    body.home .enterprise-line a:hover { background: var(--accent); }

    body.home footer.lp {
      border-top: var(--border) solid var(--fg); padding-top: 24px;
      font-size: 12px; display: flex; justify-content: space-between; color: #666;
    }
    body.home footer.lp a { color: #666; text-decoration: underline; }
    body.home footer.lp a:hover { color: var(--fg); background: var(--accent); }

    @media (max-width: 720px) {
      body.home .wrap { padding: 24px 18px 80px; }

      /* Center the brand, drop the tag — keep nav links to the right. */
      body.home header.topnav { flex-direction: column; align-items: center; gap: 16px; padding-bottom: 16px; margin-bottom: 48px; }
      body.home header.topnav .brand { text-align: center; }
      body.home header.topnav .brand .tag { display: none; }
      body.home header.topnav nav { display: flex; gap: 0; }
      body.home header.topnav nav a { margin: 0 12px; }

      body.home .steps { grid-template-columns: 1fr; }
      body.home .step { border-right: 0; border-bottom: var(--border) solid var(--fg); }
      body.home .step:last-child { border-bottom: 0; }

      body.home .demo { grid-template-columns: 1fr; }
      body.home .demo > div:first-child { border-right: 0; border-bottom: var(--border) solid var(--fg); }
      body.home .demo > div { padding: 20px; }
      body.home .demo pre { font-size: 11px; }   /* slightly smaller monospace to fit narrow viewports */

      body.home .why { grid-template-columns: 1fr; gap: 40px; }

      body.home .pricing { grid-template-columns: 1fr; }
      body.home .tier.free { border-right: 0; border-bottom: var(--border) solid var(--fg); }
      body.home .tier .head { padding: 28px 24px 24px; }
      body.home .tier .body { padding: 4px 24px; }
      body.home .tier .foot { padding: 24px 24px 28px; }
      body.home .tier .price { font-size: 64px; }

      /* Tooltip: pin to bottom of viewport so it can't overflow off-screen
         on narrow displays. Tap-and-hold on the `?` reveals it (`:active`),
         keyboard focus also works. Pure CSS, no JS = stays CSP-safe. */
      body.home .tier li .qmark:hover::after,
      body.home .tier li .qmark:focus::after,
      body.home .tier li .qmark:active::after {
        position: fixed; bottom: 16px; left: 18px; right: 18px;
        top: auto; transform: none; width: auto;
      }

      body.home .signup { padding: 20px; }
      body.home .signup form { flex-direction: column; }
      body.home .signup input[type=email] { border-right: 0; border-bottom: var(--border) solid var(--fg); }

      body.home footer.lp { flex-direction: column; gap: 8px; align-items: flex-start; }
    }
  </style>
<% end %>

<div class="wrap">

  <header class="topnav">
    <div class="brand">
      mcp-analytics
      <span class="tag">web analytics · mcp native</span>
    </div>
    <nav>
      <a href="#signup-form">Get started</a>
      <a href="#how">How</a>
      <a href="#demo">Demo</a>
      <a href="#pricing">Pricing</a>
      <a href="<%= docs_path %>">Docs</a>
    </nav>
  </header>

  <section class="hero">
    <h1>No dashboard.<br>No charts.<br><span class="accent">Just answers.</span></h1>
    <p class="sub">
      Web analytics you query through Claude. Or any MCP client.
      Ask a question in your chat, get a real answer back.
      No tabs, no dashboards, no cookies, no banner needed.
    </p>
  </section>

  <section class="signup" id="signup-form">
    <span class="label">Add to Claude</span>
    <h2>One URL. That's it.</h2>
    <p class="help">Paste this into Claude (or any MCP client) as a custom connector. It'll open a tab here to confirm — one click on a link in your email, and you're connected.</p>

    <div class="panel">
      <span class="hint">click to copy</span>
      <span class="copyable" data-copy="<%= "#{ENV.fetch('PUBLIC_BASE_URL', 'https://mcp-analytics.com')}/mcp" %>" role="button" tabindex="0"><%= "#{ENV.fetch('PUBLIC_BASE_URL', 'https://mcp-analytics.com')}/mcp" %></span>
    </div>
    <div id="copy-toast" class="toast" hidden>Copied ✓</div>

    <p class="help" style="margin-top:32px;">Or sign up first — we'll email you the connection link:</p>
    <% if flash[:alert].present? %>
      <div class="alert"><%= flash[:alert] %></div>
      <% content_for(:track_event, "signup_failed") %>
      <% content_for(:track_props, "data-prop-reason=\"#{ERB::Util.html_escape(flash[:signup_status])}\"") %>
    <% end %>
    <%= form_with url: signup_path, method: :post, local: true, html: { id: "signup-form-tag" } do |f| %>
      <%= f.email_field :email, placeholder: "you@example.com", required: true, value: flash[:email], id: "signup-email", data: { "track-input-once": "signup_form_started" } %>
      <%= f.submit "Send me the link →" %>
    <% end %>
    <small>Free forever up to 100,000 hits/month. Unlimited sites. No credit card.</small>
  </section>

  <section id="how">
    <h2 class="section" data-num="01">How it works</h2>
    <div class="steps">
      <div class="step">
        <div class="num">1</div>
        <h3>Add connector</h3>
        <p>Paste <code>mcp-analytics.com/mcp</code> into Claude as a custom connector. Claude opens a tab to authorize — confirm via the link we email you. Done forever.</p>
      </div>
      <div class="step">
        <div class="num">2</div>
        <h3>Let Claude install</h3>
        <p>Ask Claude to add the tracking snippet to your site. Strict privacy mode by default. No cookies, no banner.</p>
      </div>
      <div class="step">
        <div class="num">3</div>
        <h3>Ask anything</h3>
        <p>"How did yesterday go?" "What should I optimize?" Claude queries, explains, and suggests what to do next.</p>
      </div>
    </div>
  </section>

  <section id="demo">
    <h2 class="section" data-num="02">What it looks like</h2>
    <div class="demo">
      <div>
        <h3>Script on your site</h3>
<pre>&lt;script defer
  data-site="tb_8x9k2m4n"
  src="https://t.mcp-analytics.com/script.js"&gt;
&lt;/script&gt;</pre>
      </div>
      <div class="chat">
        <h3>In your Claude chat</h3>
        <div class="you"><div class="who">You</div>How'd yesterday go?</div>
        <div class="claude">
          <div class="who">Claude</div>
          4,120 pageviews across 2 sites yesterday, 1,860 visitors. /pricing was your top page (380 views) but bounced at 62% — about 20pp above your other landing pages. Want me to actually open /pricing and look at what might be tripping people up, or pull your last few commits in case something changed there recently?
        </div>
      </div>
    </div>
  </section>

  <section>
    <h2 class="section" data-num="03">What you lose / gain</h2>
    <div class="why">
      <div class="why-col lose">
        <h3>You lose</h3>
        <ul>
          <li>A dashboard to pay for</li>
          <li>Twelve tabs always open</li>
          <li>CSV exports at 2am</li>
          <li>Cookie banners</li>
          <li>Vendor lock-in</li>
        </ul>
      </div>
      <div class="why-col gain">
        <h3>You gain</h3>
        <ul>
          <li>Answers in your chat</li>
          <li>Claude spots what to optimize</li>
          <li>Custom events, first-class</li>
          <li>GDPR + EU hosting by default</li>
          <li>18 MCP tools for your data</li>
        </ul>
      </div>
    </div>
  </section>

  <section id="pricing">
    <h2 class="section" data-num="04">Pricing</h2>
    <div class="pricing">
      <div class="tier free">
        <div class="head">
          <span class="name">Free</span>
          <div class="price">€0<span class="per">forever</span></div>
          <p class="tagline">For your portfolio, side projects, the experiments you'll forget about.</p>
        </div>
        <div class="body">
          <ul>
            <li>100,000 hits / month</li>
            <li>Unlimited sites</li>
            <li>JS tracker</li>
            <li>Ask Claude anything about your stats</li>
            <li>3 privacy modes</li>
            <li>GDPR-clean, no banner</li>
            <li>1 year retention</li>
            <li>No credit card</li>
          </ul>
        </div>
        <div class="foot">
          <a class="cta" href="#signup-form">Start free</a>
        </div>
      </div>
      <div class="tier pro">
        <div class="head">
          <span class="name">Pro</span>
          <div class="price">€19<span class="per">/ month</span></div>
          <p class="tagline">For when the side project actually starts being a thing.</p>
        </div>
        <div class="body">
          <ul>
            <li>10M hits / month</li>
            <li>Unlimited sites</li>
            <li>See which AI agents read your site <span class="qmark" tabindex="0" data-tip="Separate ChatGPT, Claude, Perplexity, GPTBot, ClaudeBot and friends from your human visitors. Tells you which AI bots are crawling your content and how often — useful for AIEO and seeing whether your work is getting cited.">?</span><span class="soon">very soon</span></li>
            <li>Catch the traffic JS pixels miss <span class="qmark" tabindex="0" data-tip="JavaScript trackers can't see bots that don't run JS — most training crawlers, link unfurlers and headless agents. Drop in a server-side middleware (Ruby, Node) or a 1×1 pixel for static sites without a runtime, every HTTP hit gets captured.">?</span><span class="soon">soon</span></li>
            <li>Spot what your last deploy broke <span class="qmark" tabindex="0" data-tip="Add one step to your GitHub Action. After every deploy we compare the next hour against your usual baseline and flag drops in signups, conversion or key page views. Or just ask Claude 'did the last deploy break anything?'">?</span><span class="soon">soon-ish</span></li>
            <li>3 year retention</li>
            <li>Priority email support</li>
            <li>€1 per extra 1M hits</li>
          </ul>
        </div>
        <div class="foot">
          <a class="cta" href="#signup-form">Start free, upgrade later</a>
        </div>
      </div>
    </div>
    <p class="enterprise-line">
      <strong>Enterprise</strong> from €299 / month. Dedicated server, <code>query_sql</code> power tool, 99.9% SLA, custom retention, white-label tracking domain, DPA. <%= mail_to "enterprise@mcp-analytics.com", "Contact sales" %>.
    </p>
  </section>

  <footer class="lp">
    <div>© <%= Date.current.year %> mcp-analytics · <%= mail_to "enterprise@mcp-analytics.com", "enterprise" %> · <%= link_to "docs", docs_path %> · <%= link_to "terms", terms_path, rel: "nofollow" %> · <%= link_to "privacy", privacy_path, rel: "nofollow" %></div>
    <div>made in Berlin</div>
  </footer>

</div>
